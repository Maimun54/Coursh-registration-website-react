
import PropTypes from 'prop-types';

const Cart = () => {
    return (
        <div>
            <h4>This is card</h4>
        </div>
    );
};

Cart.propTypes = {
    Cart:PropTypes.object

};

export default Cart;